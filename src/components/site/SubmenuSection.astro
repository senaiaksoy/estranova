---
interface Props {
  variant?: 'cream' | 'white' | 'sand';
  /** Hero altında ilk bölümde üst çizgi */
  borderedTop?: boolean;
  class?: string;
}

const { variant = 'cream', borderedTop = true, class: className } = Astro.props;

const bg = {
  cream: 'bg-cream-soft',
  white: 'bg-white',
  sand: 'bg-cream-warm',
}[variant];
---

<section class:list={[borderedTop && 'border-t border-primary/10', bg, 'px-6 py-16 md:py-20', className]}>
  <div class="mx-auto max-w-6xl">
    <slot />
  </div>
</section>
