---
import SiteLayout from '../../layouts/SiteLayout.astro';
import SiteNavbar from '../../components/site/SiteNavbar.astro';
import SiteFooter from '../../components/site/SiteFooter.astro';
import ArticleCard from '../../components/ArticleCard.astro';
import { publishedArticles } from '../../data/articles';

const sortedArticles = [...publishedArticles].sort((a, b) => (a.date < b.date ? 1 : -1));
---

<SiteLayout
  title="Makaleler - Estranova"
  description="Estranova editoryal makale arşivi: perimenopoz, menopoz ve 40+ sağlık yazıları."
>
  <SiteNavbar currentPath="/library" />

  <main id="main-content" class="pt-24">
    <section class="px-6 py-16 md:py-20">
      <div class="max-w-6xl mx-auto">
        <header class="max-w-3xl mb-14">
          <p class="text-secondary text-xs font-bold tracking-[0.28em] uppercase mb-4">Makale Arşivi</p>
          <h1 class="text-5xl md:text-7xl font-serif leading-[1.05] text-charcoal mb-6">Tüm editoryal yazılar</h1>
          <p class="text-lg md:text-xl text-charcoal/65 leading-relaxed">
            Kanıta dayalı içerikleri kategori bazlı takip edebilir, ilgili başlıklara derinleşerek ilerleyebilirsiniz.
          </p>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          {sortedArticles.map((article) => (
            <ArticleCard article={article} href={`/article/${article.slug}`} />
          ))}
        </div>
      </div>
    </section>
  </main>

  <SiteFooter />
</SiteLayout>
